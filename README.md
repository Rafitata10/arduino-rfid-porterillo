# arduino-rfid-porterillo
RFID access control system for Arduino using an MFRC522 reader and a microSD UID database. The project demonstrates simultaneous use of hardware SPI (RFID) and SoftSPI (SD card) using the SdFat library. Includes UID reader, UID registration tool and full access control firmware.
